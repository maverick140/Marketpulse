# MarketPulse AI — Frontend Environment Configuration
# Netlify production: Set this to your Render backend URL (e.g. https://marketpulse-api.onrender.com)
# Local development: Leave blank to use the Vite local proxy (/api -> http://127.0.0.1:8000)
VITE_API_URL=
